{
  "name": "xena-portable-guardrail-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "cli": "node --env-file=.env --import tsx src/index.ts",
    "eval": "node --env-file=.env --import tsx src/eval/runner.ts"
  },
  "dependencies": {
    "@llm-guardrails/core": "^0.4.1",
    "@receptron/laya": "^0.1.2",
    "openai": "^4.104.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.0.0"
  }
}
